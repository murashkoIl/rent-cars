import { memo } from 'react'
import { CustomFilterProps } from './custom-filter.types';

const CustomFilterComponent = ({ title }: CustomFilterProps) => {
  return (
    <div>CustomFilter.componeC</div>
  )
}

export default memo(CustomFilterComponent);
